diff --git a/node_modules/@rawgraphs/rawgraphs-core/lib/index.cjs.js b/node_modules/@rawgraphs/rawgraphs-core/lib/index.cjs.js
index 15fee92..3d8c3b0 100644
--- a/node_modules/@rawgraphs/rawgraphs-core/lib/index.cjs.js
+++ b/node_modules/@rawgraphs/rawgraphs-core/lib/index.cjs.js
@@ -5633,7 +5633,12 @@ var dateTokensMap = {
   Month: "%B",
   HH: "%H",
   mm: "%M",
-  ss: "%S"
+  ss: "%S",
+  // %L = milliseconds. Must be replaced after "ss" so it cannot be shadowed.
+  SSS: "%L",
+  // %Z parses "Z" as well as "+02:00" / "-0700", so timeParse yields the
+  // correct absolute instant. A literal "Z" would be read in local time.
+  Z: "%Z"
 };
 var translateDateFormat = function translateDateFormat(df) {
   var out = new String(df);
@@ -5644,7 +5649,7 @@ var translateDateFormat = function translateDateFormat(df) {
   return out;
 }; // actual dateFormats export
 
-var formatsLabels = ["YYYY-MM-DD", "DD/MM/YYYY", "YYYY-MM", "YY-MM", "MM/YY", "MM/YYYY", "DD Month YYYY", "YYYY", "YYYY-MM-DD HH:mm:ss", "YYYY-MM-DDTHH:mm:ss"];
+var formatsLabels = ["YYYY-MM-DD", "DD/MM/YYYY", "YYYY-MM", "YY-MM", "MM/YY", "MM/YYYY", "DD Month YYYY", "YYYY", "YYYY-MM-DD HH:mm:ss", "YYYY-MM-DDTHH:mm:ss", "YYYY-MM-DDTHH:mm:ss.SSSZ"];
 var dateFormats = {};
 formatsLabels.forEach(function (label) {
   dateFormats[label] = translateDateFormat(label);
@@ -5785,6 +5790,20 @@ function getValueType(value, options) {
         dateFormat: "YYYY-MM-DDTHH:mm:ss"
       };
     }
+  } //testing "YYYY-MM-DDTHH:mm:ss.SSSZ" (ISO 8601 with milliseconds and zone)
+
+
+  if (dateParser) {
+    var _dateFormatTestIso = dateFormats["YYYY-MM-DDTHH:mm:ss.SSSZ"];
+
+    var _testDateWithFormatIso = dateParser(_dateFormatTestIso)(value);
+
+    if (_testDateWithFormatIso !== null) {
+      return {
+        type: "date",
+        dateFormat: "YYYY-MM-DDTHH:mm:ss.SSSZ"
+      };
+    }
   }
 
   return "string";
diff --git a/node_modules/@rawgraphs/rawgraphs-core/lib/index.esm.js b/node_modules/@rawgraphs/rawgraphs-core/lib/index.esm.js
index 930b58e..356f8da 100644
--- a/node_modules/@rawgraphs/rawgraphs-core/lib/index.esm.js
+++ b/node_modules/@rawgraphs/rawgraphs-core/lib/index.esm.js
@@ -5629,7 +5629,12 @@ var dateTokensMap = {
   Month: "%B",
   HH: "%H",
   mm: "%M",
-  ss: "%S"
+  ss: "%S",
+  // %L = milliseconds. Must be replaced after "ss" so it cannot be shadowed.
+  SSS: "%L",
+  // %Z parses "Z" as well as "+02:00" / "-0700", so timeParse yields the
+  // correct absolute instant. A literal "Z" would be read in local time.
+  Z: "%Z"
 };
 var translateDateFormat = function translateDateFormat(df) {
   var out = new String(df);
@@ -5640,7 +5645,7 @@ var translateDateFormat = function translateDateFormat(df) {
   return out;
 }; // actual dateFormats export
 
-var formatsLabels = ["YYYY-MM-DD", "DD/MM/YYYY", "YYYY-MM", "YY-MM", "MM/YY", "MM/YYYY", "DD Month YYYY", "YYYY", "YYYY-MM-DD HH:mm:ss", "YYYY-MM-DDTHH:mm:ss"];
+var formatsLabels = ["YYYY-MM-DD", "DD/MM/YYYY", "YYYY-MM", "YY-MM", "MM/YY", "MM/YYYY", "DD Month YYYY", "YYYY", "YYYY-MM-DD HH:mm:ss", "YYYY-MM-DDTHH:mm:ss", "YYYY-MM-DDTHH:mm:ss.SSSZ"];
 var dateFormats = {};
 formatsLabels.forEach(function (label) {
   dateFormats[label] = translateDateFormat(label);
@@ -5781,6 +5786,20 @@ function getValueType(value, options) {
         dateFormat: "YYYY-MM-DDTHH:mm:ss"
       };
     }
+  } //testing "YYYY-MM-DDTHH:mm:ss.SSSZ" (ISO 8601 with milliseconds and zone)
+
+
+  if (dateParser) {
+    var _dateFormatTestIso = dateFormats["YYYY-MM-DDTHH:mm:ss.SSSZ"];
+
+    var _testDateWithFormatIso = dateParser(_dateFormatTestIso)(value);
+
+    if (_testDateWithFormatIso !== null) {
+      return {
+        type: "date",
+        dateFormat: "YYYY-MM-DDTHH:mm:ss.SSSZ"
+      };
+    }
   }
 
   return "string";
diff --git a/node_modules/@rawgraphs/rawgraphs-core/lib/index.umd.js b/node_modules/@rawgraphs/rawgraphs-core/lib/index.umd.js
index 9a85f5d..e2e13e4 100644
--- a/node_modules/@rawgraphs/rawgraphs-core/lib/index.umd.js
+++ b/node_modules/@rawgraphs/rawgraphs-core/lib/index.umd.js
@@ -7055,7 +7055,12 @@
     Month: "%B",
     HH: "%H",
     mm: "%M",
-    ss: "%S"
+    ss: "%S",
+    // %L = milliseconds. Must be replaced after "ss" so it cannot be shadowed.
+    SSS: "%L",
+    // %Z parses "Z" as well as "+02:00" / "-0700", so timeParse yields the
+    // correct absolute instant. A literal "Z" would be read in local time.
+    Z: "%Z"
   };
   var translateDateFormat = function translateDateFormat(df) {
     var out = new String(df);
@@ -7066,7 +7071,7 @@
     return out;
   }; // actual dateFormats export
 
-  var formatsLabels = ["YYYY-MM-DD", "DD/MM/YYYY", "YYYY-MM", "YY-MM", "MM/YY", "MM/YYYY", "DD Month YYYY", "YYYY", "YYYY-MM-DD HH:mm:ss", "YYYY-MM-DDTHH:mm:ss"];
+  var formatsLabels = ["YYYY-MM-DD", "DD/MM/YYYY", "YYYY-MM", "YY-MM", "MM/YY", "MM/YYYY", "DD Month YYYY", "YYYY", "YYYY-MM-DD HH:mm:ss", "YYYY-MM-DDTHH:mm:ss", "YYYY-MM-DDTHH:mm:ss.SSSZ"];
   var dateFormats = {};
   formatsLabels.forEach(function (label) {
     dateFormats[label] = translateDateFormat(label);
@@ -7207,6 +7212,20 @@
           dateFormat: "YYYY-MM-DDTHH:mm:ss"
         };
       }
+    } //testing "YYYY-MM-DDTHH:mm:ss.SSSZ" (ISO 8601 with milliseconds and zone)
+
+
+    if (dateParser) {
+      var _dateFormatTestIso = dateFormats["YYYY-MM-DDTHH:mm:ss.SSSZ"];
+
+      var _testDateWithFormatIso = dateParser(_dateFormatTestIso)(value);
+
+      if (_testDateWithFormatIso !== null) {
+        return {
+          type: "date",
+          dateFormat: "YYYY-MM-DDTHH:mm:ss.SSSZ"
+        };
+      }
     }
 
     return "string";
